# 🚀 Быстрый старт Telegram Mini App

## Самый простой способ (5 минут)

### 1. Запусти все сервисы

Тебе нужны 3 терминала:

**Терминал 1 - Backend (main.py):**
```bash
python main.py
```

**Терминал 2 - API:**
```bash
uvicorn src.api.app:app --reload --host 0.0.0.0 --port 8000
```

**Терминал 3 - Frontend:**
```bash
cd webapp
npm run dev
```

Проверь что все работает:
- main.py: Сканер собирает дилы
- API: http://localhost:8000/health
- Frontend: http://localhost:5173

### 2. Установи ngrok

Скачай с https://ngrok.com/download или:
```bash
choco install ngrok
```

### 3. Запусти туннель

**Вариант А - Автоматически:**
```bash
start-miniapp-test.bat
```

**Вариант Б - Вручную:**
```bash
ngrok http 5173
```

Скопируй URL (типа `https://abc-123.ngrok-free.app`)

### 4. Настрой бота

1. Открой @BotFather в Telegram
2. `/mybots` → `@tongiftsbarygabot`
3. `Bot Settings` → `Menu Button` → `Configure Menu Button`
4. Отправь свой ngrok URL
5. Отправь название: `Барыга Дилов 🦝💰`

### 5. Готово! 🎉

Открой своего бота → увидишь кнопку **Menu** внизу → жми!

---

## Troubleshooting

**Проблема: "Нет дилов"**
- Проверь что main.py запущен (он собирает дилы)
- Подожди 1-2 минуты чтобы дилы накопились

**Проблема: "Cannot connect to API"**
- Убедись что API на :8000 работает
- Проверь: `curl http://localhost:8000/health`

**Проблема: "Кнопки не открывают ссылки"**
- Это работает только в настоящем Telegram, не в браузере
- Используй мобильное приложение Telegram

**Проблема: "WebSocket не подключается"**
- Это нормально для ngrok бесплатной версии
- Просто перезагружай страницу чтобы обновить дилы

---

## Что дальше?

Полная инструкция по деплою: [DEPLOY.md](./DEPLOY.md)

Там описано:
- Как задеплоить на Vercel (продакшн, бесплатно)
- Как задеплоить API на Railway/Render
- Как настроить домен
